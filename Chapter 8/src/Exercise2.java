/**
*
*	1. The first method finds the factorial of the objects in an int array. Kiwi is the seed, a is the starting
*	array
*	2. Checks if an object (grape) is located in an array (a). If it is, then the index location is returned, 
*	else, -1 is returned
*	3. Returns the amount of time integer (apple) appears in an array (a). Pear is the seed/counter.
*
*/